# DeepBIM - EnergyReports

1) Use Files.py to extract and rename Revit files from success list.

2) Place the corresponding files in Htm folder.

3) Run parser_main.py to extract the parameters and write the csv file bim_train.csv to train the model.
